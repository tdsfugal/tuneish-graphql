import mongoose from "mongoose"
import getMongooseModel from "../db/getMongooseModel"

import { getLogger } from "../util"
const logger = getLogger(__filename)

export default class SubscriptionsConnector {
  constructor() {}
  subscribe() {}
  unsubscribe() {}
}
