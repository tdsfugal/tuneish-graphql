import React from "react";
import { SimpleBackgroundView } from "/src/styles/server";

const MainBackground = () => {
  return <SimpleBackgroundView $colorToken={["core", "tertiary"]} />;
};

export default MainBackground;
